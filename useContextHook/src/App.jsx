// useContext() = React hook that allows to share values between multiple
// levels of Components without passing props through each level.
import React from "react"
import ComponentA from "./ComponentA.jsx"

function App() {

  return (
    <ComponentA/>
  );
}

export default App
